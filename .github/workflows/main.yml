name: My Action
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3

      
    - name: test start
      run: npm install jest
      working-directory: "./20181277_seungmin
    
    - name: testing
      run: npm test
      working-directory: "./20181277_seungmin"
